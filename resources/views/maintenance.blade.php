<x-guest-layout>
    <x-slot name="title">{{ __('Site unavailable') }}</x-slot>

    <h1>{{ __('Site unavailable') }}</h1>
    <p>
        {{ __('The site is currently undergoing maintenance.') }}<br />
        {{ __('Please wait a few moments and try again.') }}
    </p>
</x-guest-layout>
